server:
  port: 8000

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:

        - id: catalog-service
          uri: http://localhost:8080
          predicates:
            - Path=/product/**


        - id: order-service
          uri: http://localhost:8082
          predicates:
            - Path=/orders/**

        - id: auth-service
          uri: http://localhost:8083 # Porta do seu novo serviço
          predicates:
            - Path=/auth/**